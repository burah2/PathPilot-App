// Example import statement
const aiModule = require('./features/AI');
const port = 8080; // or any other available port
app.listen(port, () => {
  console.log(`Server is running on port ${port}`);
});
